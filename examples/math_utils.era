// Helper module: math_utils.era

fn square(x: int) -> int {
    return x * x
}

fn cube(x: int) -> int {
    return x * x * x
}

fn factorial(n: int) -> int {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}
